# CORS Proxy URL for production (Cloudflare Worker)
# Deploy the worker in /worker directory and set this to the worker URL
VITE_CORS_PROXY_URL=https://mtg-proxy-builder-cors.YOUR_SUBDOMAIN.workers.dev

# Google Drive API (for image upload feature)
# Create OAuth 2.0 Client ID at https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
